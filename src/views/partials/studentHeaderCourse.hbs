<div class="container-fluid bg-dark">
    <div class="container mt-4 mx-auto">
        <p class="text-white fw-bold fs-1">My Learning</p>
        <nav class="navbar navbar-expand-lg navbar-dark">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link fw-bold fs-4" aria-current="page" href="my-courses">My courses</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link fw-bold fs-4" href="wish-list">Wish List</a>
                </li>
            </ul>
        </nav>
    </div>
</div>

<script>
    const url = window.location.href;
    const urlArray = url.split('/');
    const page = urlArray[urlArray.length - 1];
    const navItems = document.querySelectorAll('.nav-item');
    navItems.forEach((item) => {
        const link = item.querySelector('a');
        if (link.href.includes(page)) {
            link.classList.add('active');
        }
    });
</script>